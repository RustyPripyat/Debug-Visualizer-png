# Robotic-Lib-Visualizer

Nel modulo `utils` ho aggiunto qualche metodo che crea un immagine, ne colora i pixels in base al TileType e poi tenta di mostrarla in una finestra.

#### Funziona su wayland.
